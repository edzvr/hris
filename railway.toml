[build]
builder = "NIXPACKS"

[deploy]
startCommand = "gunicorn --bind 0.0.0.0:$PORT wsgi:app"
preDeployCommand = "flask --app hris db stamp head"
restartPolicyType = "ON_FAILURE"
restartPolicyMaxRetries = 10